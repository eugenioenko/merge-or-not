diff --git a/src/graphql/resolvers/subscription.js b/src/graphql/resolvers/subscription.js
index 34f6772..7087b51 100644
--- a/src/graphql/resolvers/subscription.js
+++ b/src/graphql/resolvers/subscription.js
@@ -1,19 +1,6 @@
-const { withFilter } = require('graphql-subscriptions');
-const { AuthenticationError } = require('../errors');
-
 const Subscription = {
   orderStatusUpdated: {
-    subscribe: withFilter(
-      (_parent, _args, { pubsub, currentUser }) => {
-        if (!currentUser) {
-          throw new AuthenticationError('Not authenticated');
-        }
-        return pubsub.asyncIterator('ORDER_STATUS_UPDATED');
-      },
-      (payload, _args, { currentUser }) => {
-        return payload.orderStatusUpdated.userId === currentUser.id;
-      },
-    ),
+    subscribe: (_parent, _args, { pubsub }) => pubsub.asyncIterator('ORDER_STATUS_UPDATED'),
   },
 };
 
