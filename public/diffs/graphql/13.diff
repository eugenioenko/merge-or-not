diff --git a/src/graphql/resolvers/order.js b/src/graphql/resolvers/order.js
index f805191..f468767 100644
--- a/src/graphql/resolvers/order.js
+++ b/src/graphql/resolvers/order.js
@@ -5,24 +5,22 @@
     if (!currentUser) {
       throw new AuthenticationError('Not authenticated');
     }
-    return db.$transaction(async (tx) => {
-      const order = await tx.order.create({
-        data: { userId: currentUser.id, status: 'PENDING', total: input.total },
+    const order = await db.order.create({
+      data: { userId: currentUser.id, status: 'PENDING', total: input.total },
+    });
+    for (const item of input.items) {
+      await db.inventory.update({
+        where: { sku: item.sku },
+        data: { quantity: { decrement: item.quantity } },
       });
-      for (const item of input.items) {
-        await tx.inventory.update({
-          where: { sku: item.sku },
-          data: { quantity: { decrement: item.quantity } },
-        });
-        await tx.orderItem.create({
-          data: { orderId: order.id, sku: item.sku, quantity: item.quantity },
-        });
-      }
-      await tx.payment.create({
-        data: { orderId: order.id, amount: input.total, status: 'AUTHORIZED' },
+      await db.orderItem.create({
+        data: { orderId: order.id, sku: item.sku, quantity: item.quantity },
       });
-      return order;
+    }
+    await db.payment.create({
+      data: { orderId: order.id, amount: input.total, status: 'AUTHORIZED' },
     });
+    return order;
   },
 };
 
