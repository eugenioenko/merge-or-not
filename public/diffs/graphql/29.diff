diff --git a/src/graphql/resolvers/team.js b/src/graphql/resolvers/team.js
index 46b64a5..7d28b7b 100644
--- a/src/graphql/resolvers/team.js
+++ b/src/graphql/resolvers/team.js
@@ -1,15 +1,18 @@
+const rateLimiter = require('../middleware/rateLimiter');
 const { AuthenticationError } = require('../errors');
 
 const Mutation = {
-  inviteTeamMember: async (_parent, { email, teamId }, { db, mailer, currentUser }) => {
-    if (!currentUser) {
-      throw new AuthenticationError('Not authenticated');
-    }
-    const token = generateInviteToken();
-    await db.teamInvite.create({ data: { teamId, email, token, invitedBy: currentUser.id } });
-    await mailer.sendTeamInvite(email, token);
-    return { success: true };
-  },
+  inviteTeamMember: rateLimiter({ window: '1h', max: 20, keyBy: (ctx) => ctx.currentUser?.id })(
+    async (_parent, { email, teamId }, { db, mailer, currentUser }) => {
+      if (!currentUser) {
+        throw new AuthenticationError('Not authenticated');
+      }
+      const token = generateInviteToken();
+      await db.teamInvite.create({ data: { teamId, email, token, invitedBy: currentUser.id } });
+      await mailer.sendTeamInvite(email, token);
+      return { success: true };
+    },
+  ),
 };
 
 module.exports = { Mutation };
