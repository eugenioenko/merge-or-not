diff --git a/src/routes/webhookProxy.js b/src/routes/webhookProxy.js
index e22cb2d..24430d7 100644
--- a/src/routes/webhookProxy.js
+++ b/src/routes/webhookProxy.js
@@ -1,18 +1,13 @@
 const express = require('express');
 const router = express.Router();
 const axios = require('axios');
-const { isPrivateHost } = require('../utils/network');
 
 router.post('/integrations/preview', async (req, res) => {
   const { url } = req.body;
   if (!url || typeof url !== 'string') {
     return res.status(400).json({ error: 'url is required' });
   }
-  const parsed = new URL(url);
-  if (parsed.protocol !== 'https:' || await isPrivateHost(parsed.hostname)) {
-    return res.status(400).json({ error: 'URL not allowed' });
-  }
-  const response = await axios.get(url, { timeout: 3000, maxRedirects: 0 });
+  const response = await axios.get(url, { timeout: 3000 });
   res.json({ status: response.status, snippet: response.data.slice(0, 200) });
 });
 
