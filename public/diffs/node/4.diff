diff --git a/src/services/notifications.js b/src/services/notifications.js
index 8e1c3f0..a2d7b9c 100644
--- a/src/services/notifications.js
+++ b/src/services/notifications.js
@@ -3,15 +3,12 @@ const emailClient = require('../lib/email');
 
 async function sendOrderConfirmation(order) {
   const user = await db.users.findById(order.userId);
-  try {
-    await emailClient.send({
-      to: user.email,
-      subject: 'Order Confirmed',
-      body: renderTemplate('order-confirmation', { order, user }),
-    });
-  } catch (err) {
-    logger.error('Failed to send order confirmation', { orderId: order.id, error: err });
-    throw err;
-  }
+  emailClient.send({
+    to: user.email,
+    subject: 'Order Confirmed',
+    body: renderTemplate('order-confirmation', { order, user }),
+  });
+  return { sent: true };
 }
+
 module.exports = { sendOrderConfirmation };
