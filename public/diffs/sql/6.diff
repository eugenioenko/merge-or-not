diff --git a/queries/list_products.sql b/queries/list_products.sql
--- a/queries/list_products.sql
+++ b/queries/list_products.sql
@@ -1,15 +1,15 @@
 SELECT
     p.id,
     p.sku,
     p.name,
     p.category_id,
     p.brand_id,
     p.price_cents,
     p.in_stock,
     p.updated_at
 FROM products p
 WHERE p.is_active = true
   AND p.category_id = $1
+  AND p.id > $2
 ORDER BY p.id
-LIMIT $2
-OFFSET $3;
+LIMIT $3;
