diff --git a/migrations/0195_add_is_active_index.sql b/migrations/0195_add_is_active_index.sql
new file mode 100644
--- /dev/null
+++ b/migrations/0195_add_is_active_index.sql
@@ -0,0 +1,16 @@
+CREATE INDEX idx_users_is_active
+    ON users (is_active);
+
+CREATE INDEX idx_users_newsletter_subscribed
+    ON users (newsletter_subscribed);
+
+CREATE INDEX idx_users_marketing_opt_in
+    ON users (marketing_opt_in);
+
+CREATE INDEX idx_users_email_verified
+    ON users (email_verified);
+
+CREATE INDEX idx_users_two_factor_enabled
+    ON users (two_factor_enabled);
+
+ANALYZE users;
