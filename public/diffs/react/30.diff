diff --git a/src/hooks/usePolling.js b/src/hooks/usePolling.js
index 2b64a17..e4d4b73 100644
--- a/src/hooks/usePolling.js
+++ b/src/hooks/usePolling.js
@@ -5,15 +5,21 @@
   const [count, setCount] = useState(0)
 
   useEffect(() => {
+    let active = true
     const id = setInterval(() => {
       fetch(url)
         .then((res) => res.json())
         .then((next) => {
+          if (!active) return
           setData(next)
-          setCount(count + 1)
+          setCount((prev) => prev + 1)
         })
     }, intervalMs)
-  }, [])
+    return () => {
+      active = false
+      clearInterval(id)
+    }
+  }, [url, intervalMs])
 
   return { data, count }
 }
