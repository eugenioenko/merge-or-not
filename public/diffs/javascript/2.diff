diff --git a/src/utils/clone.js b/src/utils/clone.js
index 5c1a2f0..8e21bfa 100644
--- a/src/utils/clone.js
+++ b/src/utils/clone.js
@@ -1,3 +1,3 @@
 export function cloneDeep(value) {
-  return JSON.parse(JSON.stringify(value))
+  return structuredClone(value)
 }
@@ -8,3 +8,11 @@ export function cloneState(state) {
     items: cloneDeep(state.items),
   }
 }
+
+export function snapshotSession(session) {
+  return structuredClone({
+    user: session.user,
+    lastActive: session.lastActive,
+    preferences: session.preferences,
+  })
+}
