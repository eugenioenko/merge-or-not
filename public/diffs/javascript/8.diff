diff --git a/src/api/client.js b/src/api/client.js
index 2c4a8e1..7d9f3b5 100644
--- a/src/api/client.js
+++ b/src/api/client.js
@@ -9,3 +9,10 @@
-export async function fetchWithTimeout(url, options = {}) {
-  return fetchJson(url, options)
-}
+export async function fetchWithTimeout(url, options = {}, timeoutMs = 8000) {
+  const controller = new AbortController()
+  const timer = setTimeout(() => controller.abort(), timeoutMs)
+
+  try {
+    return await fetchJson(url, { ...options, signal: controller.signal })
+  } finally {
+    clearTimeout(timer)
+  }
+}
diff --git a/src/hooks/useSearchResults.js b/src/hooks/useSearchResults.js
index 1f5a9c2..6b8e4d0 100644
--- a/src/hooks/useSearchResults.js
+++ b/src/hooks/useSearchResults.js
@@ -7,4 +7,10 @@ export function useSearchResults(query) {
   useEffect(() => {
     if (!query) return
-    fetchWithTimeout(`/api/search?q=${encodeURIComponent(query)}`).then(setResults)
+    let cancelled = false
+    fetchWithTimeout(`/api/search?q=${encodeURIComponent(query)}`, {}, 4000).then((data) => {
+      if (!cancelled) setResults(data)
+    })
+    return () => {
+      cancelled = true
+    }
   }, [query])
