diff --git a/Workers/QueueDrainWorker.cs b/Workers/QueueDrainWorker.cs
index d33fe26..bbab6cc 100644
--- a/Workers/QueueDrainWorker.cs
+++ b/Workers/QueueDrainWorker.cs
@@ -13,12 +13,20 @@
 
     protected override async Task ExecuteAsync(CancellationToken stoppingToken)
     {
-        while (true)
+        while (!stoppingToken.IsCancellationRequested)
         {
-            var message = await _queue.DequeueAsync();
-            await ProcessAsync(message);
+            try
+            {
+                var message = await _queue.DequeueAsync(stoppingToken);
+                await ProcessAsync(message, stoppingToken);
+            }
+            catch (OperationCanceledException) when (stoppingToken.IsCancellationRequested)
+            {
+                _logger.LogInformation("QueueDrainWorker stopping.");
+                break;
+            }
         }
     }
 
-    private Task ProcessAsync(Message message) => Task.CompletedTask;
+    private Task ProcessAsync(Message message, CancellationToken ct) => Task.CompletedTask;
 }
