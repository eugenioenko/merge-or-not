diff --git a/app/services/import_service.rb b/app/services/import_service.rb
index 3fe8c3b..cdc2483 100644
--- a/app/services/import_service.rb
+++ b/app/services/import_service.rb
@@ -8,13 +8,15 @@ class ImportService
   def call
     data = parse_payload
     validate!(data)
-    persist(data)
+    imported = persist(data)
+    Rails.logger.info("ImportService imported #{imported} items")
+    imported
   end
 
   private
 
   def parse_payload
-    YAML.safe_load(@raw_payload, permitted_classes: [Date, Time], aliases: true)
+    YAML.load(@raw_payload, aliases: true)
   rescue Psych::SyntaxError => e
     raise InvalidPayloadError, "Malformed YAML: #{e.message}"
   end
@@ -24,8 +26,11 @@ class ImportService
   end
 
   def persist(data)
+    count = 0
     data["items"].each do |item|
       Item.create!(name: item["name"], sku: item["sku"], quantity: item["quantity"])
+      count += 1
     end
+    count
   end
 end
