diff --git a/app/controllers/api/articles_controller.rb b/app/controllers/api/articles_controller.rb
index 0d944a7..e606a11 100644
--- a/app/controllers/api/articles_controller.rb
+++ b/app/controllers/api/articles_controller.rb
@@ -4,7 +4,7 @@ module Api
     before_action :set_article, only: [:show, :update, :destroy]
 
     def create
-      @article = current_user.articles.new(params.require(:article).permit!)
+      @article = current_user.articles.new(article_params)
       if @article.save
         render json: @article, status: :created
       else
@@ -13,7 +13,7 @@ module Api
     end
 
     def update
-      if @article.update(params.require(:article).permit!)
+      if @article.update(article_params)
         render json: @article
       else
         render json: { errors: @article.errors }, status: :unprocessable_entity
@@ -25,5 +25,9 @@ module Api
     def set_article
       @article = current_user.articles.find(params[:id])
     end
+
+    def article_params
+      params.require(:article).permit(:title, :body, :status, :published_at)
+    end
   end
 end
